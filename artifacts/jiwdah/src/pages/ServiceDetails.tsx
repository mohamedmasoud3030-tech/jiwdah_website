import { ArrowUpRight, CheckCircle2 } from "lucide-react";
import { Link, Navigate, useParams } from "react-router";
import LenaCta from "@/components/LenaCta";
import { findService } from "@/content/services";
import PublicShell from "@/layouts/PublicShell";
import { usePreferences } from "@/providers/preferences";
export default function ServiceDetails() { const { serviceId } = useParams(); const { locale } = usePreferences(); const service = findService(serviceId); if (!service) return <Navigate to="/services" replace />; return <PublicShell><section className="lena-page lena-container lena-service-hero"><div><p className="lena-kicker">{locale === "ar" ? "مسار من حلول LENA" : "A LENA solution track"}</p><h1 className="lena-page-title">{service.title[locale]}</h1><p className="lena-lead">{service.description[locale]}</p><div className="lena-tags">{service.tags.map((tag) => <span key={tag}>{tag}</span>)}</div><div className="lena-actions"><Link className="lena-primary" to="/contact">{locale === "ar" ? "ابدأ مشروعك" : "Start a project"}<ArrowUpRight size={16} /></Link></div></div><aside className="lena-glass lena-info-panel"><p className="lena-kicker">{locale === "ar" ? "لماذا يهم" : "Why it matters"}</p><h2>{service.why[locale]}</h2></aside></section><section className="lena-section"><div className="lena-container lena-split"><article className="lena-glass lena-info-panel"><p className="lena-kicker">{locale === "ar" ? "ما الذي نبنيه" : "What we build"}</p><ul>{service.deliverables[locale].map((item) => <li key={item}><CheckCircle2 size={16} />{item}</li>)}</ul></article><article className="lena-glass lena-info-panel"><p className="lena-kicker">{locale === "ar" ? "خطوات العمل" : "How the track moves"}</p><ol>{service.process[locale].map((item, index) => <li key={item}><span>{String(index + 1).padStart(2, "0")}</span>{item}</li>)}</ol></article></div></section><LenaCta /></PublicShell>; }
